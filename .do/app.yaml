name: poc-site
services:
- name: poc-site
  environment_slug: node-js
  dockerfile_path: Dockerfile
  github:
    branch: main
    deploy_on_push: true
    repo: openclaw4861-tech/poc
  routes:
  - path: /*
  http_port: 3000
